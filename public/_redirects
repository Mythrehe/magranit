# Netlify redirect configurations
# Next.js static export generates nested directory HTML files (trailingSlash: true)
# Netlify automatically serves these clean URL paths (e.g. /courses/sap-mm/ resolving to /courses/sap-mm/index.html)
# Any missing routes will naturally fall back to the exported 404.html page
