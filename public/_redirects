# Netlify _redirects — backup for clean URL routing
# Next.js static export generates /route/index.html (trailingSlash: true)
# Netlify serves these automatically, but this ensures no 404s slip through

/*    /index.html   404
